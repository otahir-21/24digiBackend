# Node.js 20 on AL2023 uses Procfile for the command; NodeCommand is not supported.
# NODE_ENV is set in Elastic Beanstalk Environment properties.
option_settings:
  aws:elasticbeanstalk:application:environment:
    NODE_ENV: production
